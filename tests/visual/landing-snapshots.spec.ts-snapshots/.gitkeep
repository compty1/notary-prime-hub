# Snapshot baselines are generated by the Visual Regression CI workflow on first
# push (see ../README.md). This .gitkeep ensures the directory exists in git so
# the workflow can detect it as "empty" and bootstrap the PNGs into it.
