# Snapshot baselines for the booking-funnel spec are generated by the Visual
# Regression CI workflow on first push (see ../README.md). This .gitkeep
# ensures the directory exists in git so the workflow's "missing baselines"
# detector treats it as empty and bootstraps the PNGs into it.
